<!DOCTYPE html>
<html>
<head>
  <title>Rainforest</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
	<% if flash[:notice]  %>
		<p> <id = "notice"><%= flash[:notice] %></p>
	<% end %>
	<% if flash[:alert]  %>
		<p> <id = "alert"><%= flash[:alert] %></p>
	<% end %>

<%= yield %>
	<%= clear %>
	<p>
	<% if current_user %>
		Signed in as <%= current_user.name%>. <%= link_to("Log out", session_path("current"), :method => :delete) %>
	<!-- the :method => :delete above refers to the HTTP request delete, NOT THE controller action destroy-->
	<% else %>
		<%= link_to("Login", new_session_path) %> <%= link_to("Sign up", new_user_path) %>
	<% end %>
	</p>

</body>
</html>
